import Center from "@/components/center";
import { Text } from "react-native";

export default function Categories() {
  return (
    <Center>
      <Text style={{ fontSize: 24, fontWeight: "700" }}>Categories Screen</Text>
    </Center>
  );
}
