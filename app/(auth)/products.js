import Center from "@/components/center";
import { Text } from "react-native";

export default function Products() {
  return (
    <Center>
      <Text style={{ fontSize: 24, fontWeight: "700" }}>Products Screen</Text>
    </Center>
  );
}
